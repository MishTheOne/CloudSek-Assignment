[project]
name = "cloudsek-metadata-service"
version = "0.1.0"
description = "Asynchronous URL metadata inventory service"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"
